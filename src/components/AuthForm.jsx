import { useState } from 'react'
import { useAuth } from '../contexts/AuthContext'

// Segment 3: build a sign-in / sign-up form
//
// It should:
// - have email and password fields
// - toggle between "sign in" and "sign up" mode
// - call signIn or signUp from useAuth() on submit
// - display any error Firebase returns

export default function AuthForm() {
  return (
    <div>
      <p>AuthForm — coming in Segment 3</p>
    </div>
  )
}
